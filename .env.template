# Per-game .env files live inside each game directory:
#   games/phaser/jeopardy/.env
#   games/phaser/trivia/.env
#
# Each game's .env follows this pattern:
DEVVIT_SUBREDDIT=r/my_subreddit

# Playwright Testing Configuration
PLAYWRIGHT_TEST_URL=
PLAYWRIGHT_BROWSER=chromium
PLAYWRIGHT_HEADLESS=true
